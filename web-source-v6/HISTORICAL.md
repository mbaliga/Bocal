# Historical source snapshot

This directory is retained as reference only. It is not the maintained Bocal application and must not be used to build a release. Make product changes in `../web-source/`; Android packages that canonical standalone build. See `../docs/CURRENT_STATE.md`.
